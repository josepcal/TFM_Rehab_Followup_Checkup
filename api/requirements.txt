# Pinned to the exact versions validated on 2026-07-09 for reproducible builds.
# Do not loosen to >= without re-running the SCA job (pip-audit) and the test suite.
# Extras in [brackets] are load-bearing — keep them when bumping a version.
fastapi==0.136.3
uvicorn[standard]==0.49.0
sqlalchemy==2.0.50
alembic==1.18.4
psycopg2-binary==2.9.12
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
httpx==0.28.1
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0
librosa==0.11.0
praat-parselmouth==0.4.7
google-cloud-storage==3.11.0
boto3==1.43.34
pytest==9.1.0
pytest-asyncio==1.4.0
cryptography==48.0.1
